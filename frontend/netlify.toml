[build]
  base = ""          # Ha a projekted frontend mappában van, akkor ide: "frontend"
  publish = "build"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
